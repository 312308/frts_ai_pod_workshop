# L1-01-pure-function-correctness — Pure function correctness

Does a named pure/domain function match specified inputs→outputs? Prefer pointer cases to unit tests.

Add project cases under `cases/<id>/` using the three-file or pointer format. Do not copy product-specific fixtures from other programs.
