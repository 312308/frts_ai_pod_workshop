# Grading

Run the product test named in the project (from `evals.test_commands`). Pass = rule satisfied. Do not duplicate the assertion here.

```
# example — replace with config test command
<api-test-command> --tests <ExistingTestClass>
```
