Read `*-agent.md` under `.claude/agents/` (exclude README).

Pass only if each file has headings: Goal, Must / Must NOT, Input checklist (or Input), Output checklist (or Output), Execution loop (or Loop), Definition of Done, Audit and harness.
