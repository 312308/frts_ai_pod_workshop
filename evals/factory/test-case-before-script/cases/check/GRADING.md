PASS if cases md + matrix + independent br-coverage-validator PASS-before-scripts is required on implement-api, implement-ui, apply-requirements, fix-issue, generate-python-module, and validate-api-endpoints in all three twins; expand-test-coverage is design-only (no gap scripts); /run-tests requires coverage PASS and human test-package Approved. FAIL if an implementer may write the coverage file, if the matrix is treated as sufficient without case bodies, if expand generates scripts, or if /run-tests may start while coverage is FAIL or the package is not Approved.
