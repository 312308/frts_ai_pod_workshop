# Orchestrator/quality/docs do not implement features. Catalog stays 12 primaries.

Fail if orchestrator-agent Must NOT lacks 'write production code' or 'docs/harness'.
Fail if quality-agent Must NOT lacks 'write tests' or 'lower gates'.
Fail if a 13th primary `*-agent.md` appears under `.claude/agents/` (subagents under `.claude/subagents/` do not count).
