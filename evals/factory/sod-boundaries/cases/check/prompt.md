Count `*-agent.md` under `.claude/agents/` (flat). Must equal 12 (orchestrator-agent included).

Confirm orchestrator Must NOT includes production code and docs/harness.
Confirm quality-agent Must NOT includes write tests and lower gates.
Confirm documentation-agent Must NOT includes implement features.
Confirm no `.claude/commands/generate-backend.md`.
