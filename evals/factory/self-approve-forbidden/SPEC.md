# Agents cannot self-approve human gates

Fail if requirements-agent (Cursor, Claude, or Copilot) can mark a BRD Accepted, orchestrator can approve a sprint plan, or quality-agent/orchestrator can stamp test-package **Approved**.
