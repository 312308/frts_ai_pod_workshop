For every public command file in `.claude/commands/` (except README), the file must contain `artifacts/audit`.

Phase-changing commands (rule 10) must also contain that path in **Definition of Done**, not only a generic persistence sentence.
