# Completed commands write artifacts/audit.

Fail if a public command under `.claude/commands/` (except README) omits the substring `artifacts/audit` in Definition of Done or Artifact Persistence / Outputs.
