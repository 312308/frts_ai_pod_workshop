# Template hybrid enforcement

Fail if any twin can ship Java API or registry tests without persisted template vars and a PASS from `tools/templates/render_check.py`. Fail if contracts treat agent-owned as a license to rewrite controller style or registry harness files.
