PASS if all three twins lock Java structure via hybrid_policy + `render_check.py`, persist vars YAML, keep controller/CRUD/harness template-owned, and allow cited agent-owned only in the named slots (service markers / ValidationRule / new domain types; sibling test classes). FAIL if a twin can rewrite controller style, skip the check, or invent a numeric percent quota instead of the named lists.
