# Catalog lock — no duplicate execute style, no retired slash commands.

Fail if `.claude/commands/` contains: generate-backend, generate-frontend, wire-fe-be-api-sequence, write-tech-spec, gap-analysis, or any `phase-*` command.

Fail if `docs/harness/decisions.md` still asserts that packs remain as the current overlay model without a later superseding row.
