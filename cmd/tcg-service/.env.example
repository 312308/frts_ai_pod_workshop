# FOP OAuth2 client config (BRD FR-024). Template only — no real secrets here.
# Production: these are Key Vault references injected as env vars by the
# platform, never committed, never logged.

FOP_AAD_TENANT_ID=
FOP_AAD_CLIENT_ID=
FOP_AAD_CLIENT_SECRET=
FOP_AAD_SCOPE=
FOP_PARTNER_ID=
FOP_BASE_URL=
